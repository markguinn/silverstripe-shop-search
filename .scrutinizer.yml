filter:
    excluded_paths:
        - 'tests/*'
checks:
    php: true
coding_style:
    php:
        indentation:
            general:
                use_tabs: true
        spaces:
            other:
                after_type_cast: false
tools:
  external_code_coverage:
    enabled: true
    timeout: 900
